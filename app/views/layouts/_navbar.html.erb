<nav class="navbar navbar-expand-lg navbar-transparent navbar-absolute navbar-fixed-top mt-0" id='navbar'>
  <div class="container-fluid">
    <div class="navbar-wrapper">
    </div>
    <button class="navbar-toggler" type="button" data-toggle="collapse" aria-controls="navigation-index" aria-expanded="false" aria-label="Toggle navigation">
      <span class="sr-only">Toggle navigation</span>
      <span class="navbar-toggler-icon icon-bar"></span>
      <span class="navbar-toggler-icon icon-bar"></span>
      <span class="navbar-toggler-icon icon-bar"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end">
      <%if user_signed_in?%>
        <ul class="navbar-nav">
          <li class="nav-item dropdown">
            <a class="nav-link" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i class="material-icons">notifications</i>
              <%if @unfinished_quizzes&&@unfinished_quizzes.length>0%>
                <span class="notification"><%=@unfinished_quizzes.length%></span>
                <div class="ripple-container"></div>
              <%end%>
            </a>              <%if @unfinished_quizzes&&@unfinished_quizzes.length>0%>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                <a class="dropdown-item" href="/todo">You have <%=@unfinished_quizzes.length%> outstanding quizzes</a>
              </div>
            <%else%>
              <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                <a class="dropdown-item" href="/todo">You have no outstanding quizzes</a>
              </div>
            <%end%>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link" href="javascript:;" id="navbarDropdownProfile" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i class="material-icons">person</i>
              <p class="d-lg-none d-md-block">
                Account
              </p>
              <div class="ripple-container"></div>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownProfile">
              <a class="dropdown-item" href="/users/<%=current_user.id%>/edit">Update Profile</a>
              <a class="dropdown-item" href="/users/<%=current_user.id%>/password/edit">Change Password</a>
              <a class="dropdown-item" href="/todo">My Assignments</a>
              <div class="dropdown-divider"></div>
              <a class="dropdown-item" href="/session" data-method="delete">Log out</a>
            </div>
          </li>
        </ul>
      <%end%>
    </div>
  </div>
</nav>
